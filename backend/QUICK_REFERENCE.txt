╔══════════════════════════════════════════════════════════════════════════════╗
║                    F-BUDDY BACKEND - QUICK REFERENCE                         ║
╚══════════════════════════════════════════════════════════════════════════════╝

📦 START BACKEND
────────────────────────────────────────────────────────────────────────────────
cd backend
npm install
npm run dev

✅ Expected output:
   ✅ All checks passed! Server is ready to start.
   🚀 F Buddy Server running on port 5001


🧪 TEST BACKEND
────────────────────────────────────────────────────────────────────────────────
npm run test-kyc          # Automated test of complete KYC flow
npm run verify            # Check if backend is running
npm run check             # Verify configuration

Open FRONTEND_EXAMPLE.html in browser for interactive testing


🔑 API AUTHENTICATION
────────────────────────────────────────────────────────────────────────────────
1. Login to get token:
   POST http://localhost:5001/api/auth/login
   Body: { "email": "user@example.com", "password": "password123" }
   
2. Store token:
   localStorage.setItem('authToken', response.data.token);

3. Include in all KYC requests:
   headers: { 'Authorization': 'Bearer ' + token }


📋 KYC ENDPOINTS (All require Authorization header)
────────────────────────────────────────────────────────────────────────────────
GET  /api/kyc/status              - Get current KYC status
POST /api/kyc/upload-document     - Upload ID document (multipart/form-data)
POST /api/kyc/upload-selfie       - Upload selfie (multipart/form-data)
POST /api/kyc/mfa/request         - Request OTP
POST /api/kyc/mfa/verify          - Verify OTP


💻 FRONTEND INTEGRATION
────────────────────────────────────────────────────────────────────────────────
// Get token
const token = localStorage.getItem('authToken');

// Check status
axios.get('http://localhost:5001/api/kyc/status', {
  headers: { 'Authorization': `Bearer ${token}` }
});

// Upload document
const formData = new FormData();
formData.append('document', file);
formData.append('documentType', 'pan');

axios.post('http://localhost:5001/api/kyc/upload-document', formData, {
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'multipart/form-data'
  }
});

// Request OTP
axios.post('http://localhost:5001/api/kyc/mfa/request', {}, {
  headers: { 'Authorization': `Bearer ${token}` }
});

// Verify OTP
axios.post('http://localhost:5001/api/kyc/mfa/verify', 
  { otp: '123456' },
  { headers: { 'Authorization': `Bearer ${token}` } }
);


🐛 COMMON ISSUES
────────────────────────────────────────────────────────────────────────────────
❌ 401 Unauthorized
   → Include Authorization header: 'Bearer ' + token

❌ Invalid token
   → Login again to get fresh token

❌ OTP not received
   → Check backend console - OTP is always logged there

❌ File upload fails
   → Check file size (max 10MB)
   → Ensure file is JPEG, JPG, PNG, or PDF
   → Include Authorization header


📚 DOCUMENTATION
────────────────────────────────────────────────────────────────────────────────
QUICK_START.md              - Quick reference guide
KYC_INTEGRATION_GUIDE.md    - Detailed frontend integration
FIXES_SUMMARY.md            - Complete list of changes
FRONTEND_EXAMPLE.html       - Working HTML example
README.md                   - Overview and setup


🔍 DEBUGGING
────────────────────────────────────────────────────────────────────────────────
✓ Backend logs everything to console
✓ OTP codes are always printed
✓ Authentication attempts are logged
✓ File uploads are tracked
✓ OCR processing is logged


⚙️ ENVIRONMENT VARIABLES (.env)
────────────────────────────────────────────────────────────────────────────────
PORT=5001
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your_secret_key
JWT_EXPIRE=30d

# Optional - for email OTP
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD=your_app_password


✅ WHAT'S FIXED
────────────────────────────────────────────────────────────────────────────────
✓ 401 Unauthorized errors - Enhanced auth middleware
✓ File upload issues - Auto-creates directories
✓ OCR processing - Better logging and validation
✓ OTP system - Always logs to console
✓ Complete testing suite
✓ Comprehensive documentation


🎯 NEXT STEPS
────────────────────────────────────────────────────────────────────────────────
1. Start backend: npm run dev
2. Test backend: npm run test-kyc
3. Update frontend: Add Authorization header
4. Test complete flow
5. Deploy!


═══════════════════════════════════════════════════════════════════════════════
For detailed instructions, see: KYC_INTEGRATION_GUIDE.md
═══════════════════════════════════════════════════════════════════════════════
